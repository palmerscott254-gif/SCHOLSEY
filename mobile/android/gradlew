#!/bin/sh

# Simple Gradle Wrapper - uses extracted gradle binary

SCRIPT_DIR="$( cd "$( dirname "$0" )" && pwd )"
GRADLE_BIN="$SCRIPT_DIR/gradle/wrapper/gradle-8.2/bin/gradle"

if [ ! -x "$GRADLE_BIN" ]; then
    echo "Error: Gradle not found at $GRADLE_BIN"
    exit 1
fi

exec "$GRADLE_BIN" "$@"
